<div class="row">
  <div class="col">
    <%= form_for(:opinion, url: new_jlt_path) do |f| %>
      <%= f.label :text, "Just Like That!" %>
      <%= f.text_area :text, class: 'form-control' %>

      <%= f.submit 'Post JLT', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>